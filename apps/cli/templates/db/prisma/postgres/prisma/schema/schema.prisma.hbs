generator client {
  provider = "prisma-client"
  output   = "../generated"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
  {{#if (eq dbSetup "supabase")}}
  directUrl = env("DIRECT_URL")
  {{/if}}
}
